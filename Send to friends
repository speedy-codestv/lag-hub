local RunService = game:GetService("RunService")

print("1 FPS mode activated")

RunService.RenderStepped:Connect(function()
	-- Freeze the frame for 1 second
	local start = tick()
	while tick() - start < 1 do
		task.wait()
	end
end)
